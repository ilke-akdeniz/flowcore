# Binaries
# Go names a binary after its directory, so each module's needs its own entry.
/flowcore
/client/client
*.exe

# Env / local secrets
.env
.env.local

# Test & coverage output
*.out
coverage.*

# OS noise
.DS_Store

# Editor
.vscode/*
!.vscode/settings.json
